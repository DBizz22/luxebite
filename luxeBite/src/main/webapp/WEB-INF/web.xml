<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
    version="5.0"
    metadata-complete="false">

    <display-name>LuxeBite Restaurant Web App</display-name>

    <session-config>
        <session-timeout>5</session-timeout>        <!-- 10 minutes inactivity = auto logout -->
        <cookie-config>
            <http-only>true</http-only>              <!-- block XSS session theft -->
            <secure>true</secure>                    <!-- only over HTTPS (disable only on localhost) -->
        </cookie-config>
        <tracking-mode>COOKIE</tracking-mode>        <!-- no jsessionid in URLs ever -->
    </session-config>

    <welcome-file-list>
        <welcome-file>login</welcome-file>
    </welcome-file-list>

</web-app>